<UserControl x:Class="Projekt_WPF_TODO_app.Controls.WorkTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Projekt_WPF_TODO_app.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             MouseLeftButtonUp="WorkTask_Click">
    <StackPanel Orientation="Horizontal">
        <CheckBox IsChecked="{Binding IsSelected}" />
        <StackPanel>
            <TextBlock Text="{Binding TaskTitle}"/>
            <TextBlock Text="{Binding TaskDescription}"/>
            <TextBlock Text="{Binding TaskPriority}"/>
            <TextBlock Text="{Binding TaskDueDate}"/>
            <TextBlock Text="{Binding TaskStartDate}"/>
            <TextBlock Text="{Binding TaskCompletionDate}"/>
        </StackPanel>
    </StackPanel>
</UserControl>
